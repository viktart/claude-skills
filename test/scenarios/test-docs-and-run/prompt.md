First run /ios-update-test-docs, then run /ios-run-tests DemoAppUITests.

This is a non-interactive test run — no user is available to answer questions.
Where the skills say to ask or confirm, use these pre-supplied answers instead:

- Simulator: if one is already booted, use it without asking. Otherwise boot the
  newest available iPhone simulator and use that.
- The app may not be installed on the simulator yet — that's expected; fall back
  to reading the bundle ID from the project as the skill describes, or build first.
- Anything else: choose the sensible default and continue.

Run both skills to completion, including extracting and describing the screenshots.
